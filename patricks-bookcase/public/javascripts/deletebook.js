

async function deleteBook(deleteid){
    // const res = await fetch(`/library/${deleteid}`, {
    //     method: "DELETE",
    //     headers: { "Content-Type": "application/json" },
    // });
    // console.log(res)
    // fetch(`/library/${deleteid}`, res.method) 
    // let data = await (response => {response.json()})
     // Manipulate the data retrieved back, if we want to do something with it

    // let response = await fetch(`library/${deleteid}`);
    // let data = await response.json();
    // console.log(data)
    // window.confirm(`Are you sure you want to delete ${data[0].title}?`)
}

